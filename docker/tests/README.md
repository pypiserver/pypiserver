<!-- -*-GFM-*- -->

# Docker Tests

This directory contains tests for the docker image. Note that for these tests to
run, the pytest process must be able to run `docker`. If you are on a system
where that requires `sudo`, you will need to run the tests with `sudo`.

