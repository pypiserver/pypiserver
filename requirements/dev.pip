## Pip-dependencies for developing the project.
#
# Install it with:
#   pip install -r requirements/dev.pip

-r exe.pip
-r test.pip

black
docopt  # For `/bin/bumpver.py`.
types-setuptools==75.8.0.20250225 # For mypy stubs
mypy; implementation_name == 'cpython'
